# lilToon UnityGLTF 扩展

这是给 UnityGLTF 使用的导入扩展包，用来接住 Hollow/lil 系列材质契约。

它应该和这些包放在同一套项目体系里：

- `jp.lilxyzw.liltoon`
- `jp.lilxyzw.lilpbr`
- `jp.lilxyzw.liltoon.urp.extensions`

这个包会读取 glTF 材质扩展里携带的项目材质契约，让 UnityGLTF 在导入阶段就能保留这些信息，避免先导成通用材质后再转换时丢失自定义参数。

## 当前范围

- 注册 UnityGLTF 导入插件：`HO_materials_principled_lil`。
- 读取 `HO_materials_principled_lil`，并兼容旧名 `HO_materials_openpbr_lil`。
- 把原始 extension JSON 保存成导入子资源，方便后续重建材质。
- 依赖 UnityGLTF/GLTFSerialization 的 `DefaultExtension` 行为保留未知 extension JSON。
- 解析 Blender 侧 `HoGLTF` 节点组写出的 `HO_materials_principled_lil.hogltf` 输入。
- 提供临时的 `UnityGLTF/PBRGraph -> URP/Lit` fallback 映射。

下一步是把 fallback 替换成真正的 lilToon/lilPBR 映射。

## 安装

在项目 `Packages/manifest.json` 里同时加入 UnityGLTF 和本包：

```json
{
  "dependencies": {
    "org.khronos.unitygltf": "https://github.com/KhronosGroup/UnityGLTF.git",
    "jp.lilxyzw.liltoon.unitygltf.extensions": "file:D:/Unity_Fork/Liltoon-UnityGLTF-Extensions"
  }
}
```

也可以在 Unity Package Manager 里先安装 UnityGLTF，再添加本地包：

```text
D:/Unity_Fork/Liltoon-UnityGLTF-Extensions
```

本包通过 asmdef 引用 UnityGLTF 的 `UnityGLTFScripts` 和 `GLTFSerialization` 程序集，所以 UnityGLTF 必须先在同一个项目里可用。

## UnityGLTF 注册位置

UnityGLTF 会扫描所有已编译程序集里继承自 `UnityGLTF.Plugins.GLTFImportPlugin` 的类。包编译通过后，插件会出现在：

```text
Project Settings > UnityGLTF > Import > Import Extensions and Plugins
```

Editor 导入 `.gltf` 资产时，UnityGLTF 会把插件设置保存在对应 importer 上。运行时导入则使用 Project Settings 里的默认插件状态。

## 参考源码

当前实现参考 UnityGLTF 自己的插件模式。重点文件：

```text
Runtime/Scripts/Plugins/Core/GltfPlugin.cs
Runtime/Scripts/Plugins/Core/GltfImportPlugin.cs
Runtime/Scripts/Plugins/Core/ImportContext.cs
Runtime/Scripts/GLTFSettings.cs
Runtime/Scripts/SceneImporter/ImporterMaterials.cs
Runtime/Scripts/Plugins/MaterialExtensionsImport.cs
```

当前 BREAK_URP 项目里的参考路径：

```text
D:/Unity_Project/BREAK_URP/Library/PackageCache/org.khronos.unitygltf@ce6a5dce952d
```

## 为什么需要这个包

导入后再右键转换 `.mat`，只能看到 Unity 材质上已经写入的 shader property。

如果 UnityGLTF 在导入时忽略了 `HO_materials_principled_lil` 这类自定义 glTF extension，后处理转换器就无法恢复这些原始 JSON。

这个包把转换入口前移到 UnityGLTF 导入流程里，在 `GLTFMaterial.Extensions` 还存在时读取并保存契约。
